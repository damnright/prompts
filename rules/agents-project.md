# {{项目名}} — Agent 规则

<!--
起步模板:填完所有 {{占位符}},删除用不用的段落,然后删除本注释;提交前全文搜索 "{{" 确认无残留。
给 Agent 的守则:若本文件仍含 {{,视为项目未配置——命令段不得猜测执行,示例段不得当作事实,先向用户确认。
-->

## 项目概览

- 一句话定位:{{这个项目是什么、给谁用、解决什么问题}}
- 技术栈:{{框架 / 语言 / 包管理器}}
- 分工参考:{{可选,如设计走 gemini、架构与后端走 codex、前端实现走 claude}}

## 常用命令

<!-- 示例(pnpm 项目):pnpm install / pnpm dev / pnpm test / pnpm lint / pnpm build。按实际替换,填完删除本注释。 -->

- 安装依赖:{{命令}}
- 本地开发:{{命令}}
- 测试(改动后必须通过):{{命令}}
- Lint / 格式化:{{命令}}
- 构建:{{命令}}

## 目录结构

{{5-10 行,只写无法从命名推断的职责与依赖方向,如:
- src/api/ - 接口定义,由代码生成,不要手改
- src/modules/canvas/ - 画布核心,其他模块只经其导出接口访问}}

## 代码约定

- {{只写与默认惯例不同的约定,如:组件一律 <script setup> + PascalCase 命名}}
- {{错误处理约定,如:请求错误统一走 http/ 封装,组件内不单独 catch}}
- {{禁用项,如:不引入新 UI 组件库;不使用 any}}

## 外部工具(按需保留)

{{仅列出本项目实际配置的工具(与 .mcp.json / 工具配置保持一致),未配置的删除本段,如:
- playwright:UI 自动化与端到端验证
- figma:读取设计稿,项目文件见 {{链接}}}}

## 禁止事项

- {{项目红线,如:不修改 src/api/ 下的生成代码;不升级依赖大版本;不改 .env.example 之外的配置文件}}
