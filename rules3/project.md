# {{项目名}} — 项目 Agent 规则

<!--
填表说明：
- 三态协议：字段填写后即为事实；不适用的整段直接删除；暂未填的保留 {{ 占位符——空缺字段不得推断填充，宁留 {{。
- 仅填写项目真实可用的命令与事实，不为补全模板而虚构。
- 提交前全文搜索 "{{" 确认无残留。
-->

> Agent 守则：本文件残留 `{{` 的段落视为未配置——命令不得猜测执行、示例不得当作事实，先向用户确认。本文件约定优先于全局默认，但不越过授权与安全红线。

## 项目概览

- 一句话定位：{{是什么、给谁用、解决什么问题}}
- 主要用户与场景：{{}}
- 技术栈：{{框架 / 语言 / 包管理器}}
- 运行时与版本：{{如 node 20 / python 3.12}}
- 支持平台与环境：{{浏览器 / OS / 部署目标}}
- 分工参考：{{可选，如设计走 gemini、架构与后端走 codex、前端实现走 claude}}

## 常用命令

- 安装依赖：{{命令}}
- 本地开发：{{命令}}
- 测试：{{命令}}
- 类型检查：{{命令}}
- Lint / 格式化：{{命令}}
- 构建：{{命令}}
- E2E / UI 验证：{{命令}}

## 架构

- 主入口：{{}}
- 前端：{{}}
- 后端 / 服务：{{}}
- 数据库 / 存储：{{}}
- 共享模块：{{}}
- 外部服务：{{}}
- 边界与公共 API：{{哪些模块边界与公共接口不可随意触碰；全局规则以本字段判定"跨模块"改动}}

## 项目约定

只记录无法由 formatter、lint、类型系统或现有代码可靠推断的约定。

- 命名与组织：{{}}
- UI / UX：{{}}
- API / 数据库：{{}}
- 错误处理 / 日志：{{}}
- 其他：{{}}

## 约束

- 必须支持：{{}}
- 不得破坏：{{}}
- 安全 / 合规：{{}}
- 性能 / 兼容：{{}}
- 禁止变更：{{如不修改 src/api/ 生成代码、不升级依赖大版本}}
- 发布 / 分支约束：{{}}

## 外部工具

仅列出实际配置的工具（与 .mcp.json / 工具配置保持一致），未配置则删除本段。

- {{工具名}}：{{用途}}

## 文档

- 主文档：{{链接}}
- 架构 / API 文档：{{链接}}
- 部署 / 运维手册：{{链接}}
